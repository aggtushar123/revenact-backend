# SOC 2 Control Exceptions

A requirement that cannot be met as written is recorded here rather than ignored.
Every exception needs a compensating control, an approver, and an expiry. Expired
exceptions are findings. Review this file quarterly.

| ID | Requirement | Scope (paths/services) | Rationale | Compensating control | Approver | Opened | Expires | Status |
|----|-------------|------------------------|-----------|----------------------|----------|--------|---------|--------|
| EX-001 | AUTH-03 (MFA for privileged users) | `services/accounts` — every login | Product is in development with no paying customers; the only production tenant is the demo organisation. TOTP is scheduled before the first customer is onboarded. | 12-character minimum with common-password and similarity checks on every path that sets a password (AUTH-04); per-IP and per-account login throttling (AUTH-06); every login attempt, success or failure, in the audit log (LOG-01); refresh tokens single-use with rotation and blacklist (AUTH-05); admin sessions expire after 30 min idle. | [TODO: name] | 2026-09-15 | 2027-03-31 | open |
| EX-002 | CHG-01 (branch protection), CHG-02 (required checks) | GitHub `aggtushar123/revenact-backend`, `aggtushar123/revenact-frontend` | Private repositories on the GitHub Free plan cannot have branch protection or rulesets (`gh api repos/…/rulesets` → 403 "Upgrade to GitHub Pro or make this repository public"). **Closed 2026-09-16:** both repositories were made public and a ruleset now protects `main` (pull request required, all CI and security checks required, no force push or deletion, no bypass). | `ci.yml` and `soc2-gates.yml` (tests, lint, gitleaks, semgrep, pip-audit / npm audit, heuristic scan) run on every push to `main` and every pull request and are visible in the Actions tab; pre-commit gitleaks and staged scan locally; CODEOWNERS and the PR template are in place so protection applies the day the plan changes; one maintainer today. Close by upgrading the plan or making the repos public. | [TODO: name] | 2026-09-15 | 2027-03-31 | closed 2026-09-16 |
| EX-003 | DATA-03 (TLS in transit — internal hops) | `revenact-infra/deploy/docker-compose.prod.yml`: caddy→backend, backend→db, backend→redis | All containers run on one VM inside a Docker bridge network that is not routable from outside the host; TLS between containers on the same host adds certificate management with no reduction in exposure. Public traffic is TLS at Caddy. | Only 80/443 are published; Postgres and Redis publish no ports; the NSG allows SSH only from `ssh_allowed_cidrs` (INFRA-02); Let's Encrypt at the edge with HSTS. Revisit if any service moves to a second host or a managed database. | [TODO: name] | 2026-09-15 | 2027-09-15 | open |
